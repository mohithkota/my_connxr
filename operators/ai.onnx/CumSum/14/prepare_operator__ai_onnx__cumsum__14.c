//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx__cumsum__14.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx__cumsum__14(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    // Onnx__TensorProto *i_x = searchInputByName(ctx, 0);
    // Onnx__TensorProto *i_axis = searchInputByName(ctx, 1);

    // TRACE_TENSOR(2, true, i_x);
    // TRACE_TENSOR(2, true, i_axis);

    // Onnx__AttributeProto *a_exclusive = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"exclusive");
    // Onnx__AttributeProto *a_reverse = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"reverse");

    // TRACE_ATTRIBUTE(2, a_exclusive, a_exclusive);
    // TRACE_ATTRIBUTE(2, a_reverse, a_reverse);

    // Onnx__TensorProto *o_y = searchOutputByName(ctx, 0);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    

    // context_operator__ai_onnx__cumsum__14 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx__cumsum__14));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    

    TRACE_VAR(2, true, op_ctx->exclusive, "%" PRId64);
    TRACE_VAR(2, true, op_ctx->reverse, "%" PRId64);

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */


    /* MALLOC OUTPUT TENSORS HERE */

    // mallocTensorData(o_y);

    // TRACE_TENSOR(2, true, o_y);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    // ctx->executer = resolve_operator__ai_onnx__cumsum__14(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    return OP_ENOSYS;
    // return OP_OK;
}