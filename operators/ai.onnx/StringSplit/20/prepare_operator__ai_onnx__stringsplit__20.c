//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx__stringsplit__20.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx__stringsplit__20(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    // Onnx__TensorProto *i_X = searchInputByName(ctx, 0);

    // TRACE_TENSOR(2, true, i_X);

    // Onnx__AttributeProto *a_delimiter = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"delimiter");
    // Onnx__AttributeProto *a_maxsplit = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"maxsplit");

    // TRACE_ATTRIBUTE(2, a_delimiter, a_delimiter);
    // TRACE_ATTRIBUTE(2, a_maxsplit, a_maxsplit);

    // Onnx__TensorProto *o_Y = searchOutputByName(ctx, 0);
    // Onnx__TensorProto *o_Z = searchOutputByName(ctx, 1);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    

    // context_operator__ai_onnx__stringsplit__20 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx__stringsplit__20));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    

    TRACE_VAR(2, true, op_ctx->delimiter, "\"%s\"");
    TRACE_VAR(2, true, op_ctx->maxsplit, "%" PRId64);

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */


    /* MALLOC OUTPUT TENSORS HERE */

    // mallocTensorData(o_Y);
    // mallocTensorData(o_Z);

    // TRACE_TENSOR(2, true, o_Y);
    // TRACE_TENSOR(2, true, o_Z);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    // ctx->executer = resolve_operator__ai_onnx__stringsplit__20(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    return OP_ENOSYS;
    // return OP_OK;
}