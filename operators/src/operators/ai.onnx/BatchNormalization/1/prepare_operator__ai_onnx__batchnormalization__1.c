//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx__batchnormalization__1.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx__batchnormalization__1(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    // Onnx__TensorProto *i_X = searchInputByName(ctx, 0);
    // Onnx__TensorProto *i_scale = searchInputByName(ctx, 1);
    // Onnx__TensorProto *i_B = searchInputByName(ctx, 2);
    // Onnx__TensorProto *i_mean = searchInputByName(ctx, 3);
    // Onnx__TensorProto *i_var = searchInputByName(ctx, 4);

    // TRACE_TENSOR(2, true, i_X);
    // TRACE_TENSOR(2, true, i_scale);
    // TRACE_TENSOR(2, true, i_B);
    // TRACE_TENSOR(2, true, i_mean);
    // TRACE_TENSOR(2, true, i_var);

    // Onnx__AttributeProto *a_consumed_inputs = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"consumed_inputs");
    // Onnx__AttributeProto *a_epsilon = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"epsilon");
    // Onnx__AttributeProto *a_is_test = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"is_test");
    // Onnx__AttributeProto *a_momentum = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"momentum");
    // Onnx__AttributeProto *a_spatial = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"spatial");

    // TRACE_ATTRIBUTE(2, true, a_consumed_inputs);
    // TRACE_ATTRIBUTE(2, a_epsilon, a_epsilon);
    // TRACE_ATTRIBUTE(2, a_is_test, a_is_test);
    // TRACE_ATTRIBUTE(2, a_momentum, a_momentum);
    // TRACE_ATTRIBUTE(2, a_spatial, a_spatial);

    // Onnx__TensorProto *o_Y = searchOutputByName(ctx, 0);
    // Onnx__TensorProto *o_mean = searchOutputByName(ctx, 1);
    // Onnx__TensorProto *o_var = searchOutputByName(ctx, 2);
    // Onnx__TensorProto *o_saved_mean = searchOutputByName(ctx, 3);
    // Onnx__TensorProto *o_saved_var = searchOutputByName(ctx, 4);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    

    // context_operator__ai_onnx__batchnormalization__1 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx__batchnormalization__1));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    

    TRACE_ARRAY(2, true, op_ctx->consumed_inputs, , op_ctx->n_consumed_inputs, "%" PRId64);
    TRACE_VAR(2, true, op_ctx->epsilon, "%f");
    TRACE_VAR(2, true, op_ctx->is_test, "%" PRId64);
    TRACE_VAR(2, true, op_ctx->momentum, "%f");
    TRACE_VAR(2, true, op_ctx->spatial, "%" PRId64);

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */


    /* MALLOC OUTPUT TENSORS HERE */

    // mallocTensorData(o_Y);
    // mallocTensorData(o_mean);
    // mallocTensorData(o_var);
    // mallocTensorData(o_saved_mean);
    // mallocTensorData(o_saved_var);

    // TRACE_TENSOR(2, true, o_Y);
    // TRACE_TENSOR(2, mean, o_mean);
    // TRACE_TENSOR(2, var, o_var);
    // TRACE_TENSOR(2, saved_mean, o_saved_mean);
    // TRACE_TENSOR(2, saved_var, o_saved_var);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    // ctx->executer = resolve_operator__ai_onnx__batchnormalization__1(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    return OP_ENOSYS;
    // return OP_OK;
}