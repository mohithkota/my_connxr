//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx__if__19.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx__if__19(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    // Onnx__TensorProto *i_cond = searchInputByName(ctx, 0);

    // TRACE_TENSOR(2, true, i_cond);

    // Onnx__AttributeProto *a_else_branch = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"else_branch");
    // Onnx__AttributeProto *a_then_branch = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"then_branch");

    // TRACE_ATTRIBUTE(2, true, a_else_branch);
    // TRACE_ATTRIBUTE(2, true, a_then_branch);

    // Onnx__TensorProto *o_outputs = searchOutputByName(ctx, 0);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    

    // context_operator__ai_onnx__if__19 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx__if__19));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    

    TRACE_GRAPH(2, true, op_ctx->else_branch);
    TRACE_GRAPH(2, true, op_ctx->then_branch);

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */


    /* MALLOC OUTPUT TENSORS HERE */

    // mallocTensorData(o_outputs);

    // TRACE_TENSOR(2, true, o_outputs);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    // ctx->executer = resolve_operator__ai_onnx__if__19(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    return OP_ENOSYS;
    // return OP_OK;
}