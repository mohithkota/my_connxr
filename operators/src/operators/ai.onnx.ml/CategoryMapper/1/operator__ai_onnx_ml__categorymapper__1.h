//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorHeader.py
# ifndef OPERATOR_OPERATOR__AI_ONNX_ML__CATEGORYMAPPER__1_H
# define OPERATOR_OPERATOR__AI_ONNX_ML__CATEGORYMAPPER__1_H

# include "operators/operator.h"
# include "operators/operator_stub.h"
# include "operators/operator_info.h"

/**
 * ai.onnx.ml operator 'CategoryMapper' version 1
 *
 * @param[in]  ctx  Operator context
 * @return          Status code
 *
 * Converts strings to integers and vice versa.<br>
 *     Two sequences of equal length are used to map between integers and strings,
 *     with strings and integers at the same index detailing the mapping.<br>
 *     Each operator converts either integers to strings or strings to integers, depending
 *     on which default value attribute is provided. Only one default value attribute
 *     should be defined.<br>
 *     If the string default value is set, it will convert integers to strings.
 *     If the int default value is set, it will convert strings to integers.
 * 
 * Constraint T1:
 *   The input must be a tensor of strings or integers, either [N,C] or [C].
 *   Allowed Types: tensor_int64, tensor_string
 * 
 * Constraint T2:
 *   The output is a tensor of strings or integers. Its shape will be the same
 *   as the input shape.
 *   Allowed Types: tensor_int64, tensor_string
 * Input T1 X:
 *   Input data
 *   Allowed Types: tensor_int64, tensor_string
 * Output T2 Y:
 *   Output data. If strings are input, the output values are integers, and
 *   vice versa.
 *   Allowed Types: tensor_int64, tensor_string
 * Attribute INTS cats_int64s (optional):
 *   The integers of the map. This sequence must be the same length as the
 *   'cats_strings' sequence.
 * 
 * Attribute STRINGS cats_strings (optional):
 *   The strings of the map. This sequence must be the same length as the
 *   'cats_int64s' sequence
 * 
 * Attribute INT default_int64 (optional):
 *   An integer to use when an input string value is not found in the
 *   map.<br>One and only one of the 'default_*' attributes must be defined.
 * 
 * Attribute STRING default_string (optional):
 *   A string to use when an input integer value is not found in the
 *   map.<br>One and only one of the 'default_*' attributes must be defined.
 *
 * @since version 1
 *
 * @see github/workspace/onnx/defs/traditionalml/defs.cc:159
 * @see https://github.com/onnx/onnx/blob/master/docs/Operators-ml.md#CategoryMapper
 */

operator_status
prepare_operator__ai_onnx_ml__categorymapper__1(
    node_context *ctx
);

extern operator_info info_operator__ai_onnx_ml__categorymapper__1;

typedef struct {
// no attributes
} context_operator__ai_onnx_ml__categorymapper__1;

operator_executer
resolve_operator__ai_onnx_ml__categorymapper__1(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__categorymapper__1(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__categorymapper__1__T1_tensor_int64(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__categorymapper__1__T1_tensor_string(
    node_context *ctx
);

# endif