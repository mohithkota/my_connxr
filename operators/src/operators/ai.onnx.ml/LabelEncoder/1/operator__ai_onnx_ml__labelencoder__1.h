//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorHeader.py
# ifndef OPERATOR_OPERATOR__AI_ONNX_ML__LABELENCODER__1_H
# define OPERATOR_OPERATOR__AI_ONNX_ML__LABELENCODER__1_H

# include "operators/operator.h"
# include "operators/operator_stub.h"
# include "operators/operator_info.h"

/**
 * ai.onnx.ml operator 'LabelEncoder' version 1
 *
 * @param[in]  ctx  Operator context
 * @return          Status code
 *
 * Converts strings to integers and vice versa.<br>
 *     If the string default value is set, it will convert integers to strings.
 *     If the int default value is set, it will convert strings to integers.<br>
 *     Each operator converts either integers to strings or strings to integers, depending
 *     on which default value attribute is provided. Only one default value attribute
 *     should be defined.<br>
 *     When converting from integers to strings, the string is fetched from the
 *     'classes_strings' list, by simple indexing.<br>
 *     When converting from strings to integers, the string is looked up in the list
 *     and the index at which it is found is used as the converted value.
 * 
 * Constraint T1:
 *   The input type must be a tensor of integers or strings, of any shape.
 *   Allowed Types: tensor_int64, tensor_string
 * 
 * Constraint T2:
 *   The output type will be a tensor of strings or integers, and will have
 *   the same shape as the input.
 *   Allowed Types: tensor_int64, tensor_string
 * Input T1 X:
 *   Input data.
 *   Allowed Types: tensor_int64, tensor_string
 * Output T2 Y:
 *   Output data. If strings are input, the output values are integers, and
 *   vice versa.
 *   Allowed Types: tensor_int64, tensor_string
 * Attribute STRINGS classes_strings (optional):
 *   A list of labels.
 * 
 * Attribute INT default_int64 (optional):
 *   An integer to use when an input string value is not found in the
 *   map.<br>One and only one of the 'default_*' attributes must be defined.
 * 
 * Attribute STRING default_string (optional):
 *   A string to use when an input integer value is not found in the
 *   map.<br>One and only one of the 'default_*' attributes must be defined.
 *
 * @since version 1
 *
 * @see github/workspace/onnx/defs/traditionalml/old.cc:22
 * @see https://github.com/onnx/onnx/blob/master/docs/Operators-ml.md#LabelEncoder
 */

operator_status
prepare_operator__ai_onnx_ml__labelencoder__1(
    node_context *ctx
);

extern operator_info info_operator__ai_onnx_ml__labelencoder__1;

typedef struct {
// no attributes
} context_operator__ai_onnx_ml__labelencoder__1;

operator_executer
resolve_operator__ai_onnx_ml__labelencoder__1(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__labelencoder__1(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__labelencoder__1__T1_tensor_int64(
    node_context *ctx
);

operator_status
execute_operator__ai_onnx_ml__labelencoder__1__T1_tensor_string(
    node_context *ctx
);

# endif