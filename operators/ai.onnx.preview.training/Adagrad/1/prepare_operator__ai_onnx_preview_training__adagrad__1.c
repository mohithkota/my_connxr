//this file was generated by ../../../../../../../../connx/connx_ajit/scripts/onnx_generator/OperatorTemplate.py
#include "operator__ai_onnx_preview_training__adagrad__1.h"
#include "tracing.h"
#include "utils.h"

operator_status
prepare_operator__ai_onnx_preview_training__adagrad__1(
    node_context *ctx
)
{
    TRACE_ENTRY(1);

    TRACE_NODE(2, true, ctx->onnx_node);

    /* UNCOMMENT AS NEEDED */

    // Onnx__TensorProto *i_R = searchInputByName(ctx, 0);
    // Onnx__TensorProto *i_T = searchInputByName(ctx, 1);
    // Onnx__TensorProto *i_inputs = searchInputByName(ctx, 2);

    // TRACE_TENSOR(2, true, i_R);
    // TRACE_TENSOR(2, true, i_T);
    // TRACE_TENSOR(2, true, i_inputs);

    // Onnx__AttributeProto *a_decay_factor = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"decay_factor");
    // Onnx__AttributeProto *a_epsilon = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"epsilon");
    // Onnx__AttributeProto *a_norm_coefficient = searchAttributeNyName(ctx->onnx_node->n_attribute,ctx->onnx_node->attribute,"norm_coefficient");

    // TRACE_ATTRIBUTE(2, a_decay_factor, a_decay_factor);
    // TRACE_ATTRIBUTE(2, a_epsilon, a_epsilon);
    // TRACE_ATTRIBUTE(2, a_norm_coefficient, a_norm_coefficient);

    // Onnx__TensorProto *o_outputs = searchOutputByName(ctx, 0);

    /* ALLOCATE AND INITIALIZE CONTEXT HERE IF NEEDED */

    

    // context_operator__ai_onnx_preview_training__adagrad__1 *op_ctx = NULL;
    // op_ctx = malloc(sizeof(context_operator__ai_onnx_preview_training__adagrad__1));
    // TRACE_FATAL(0 , !op_ctx, "could not allocate executer_context");

    

    TRACE_VAR(2, true, op_ctx->decay_factor, "%f");
    TRACE_VAR(2, true, op_ctx->epsilon, "%f");
    TRACE_VAR(2, true, op_ctx->norm_coefficient, "%f");

    /* INITIALIZE OUTPUTS DATA_TYPE AND SHAPE HERE */


    /* MALLOC OUTPUT TENSORS HERE */

    // mallocTensorData(o_outputs);

    // TRACE_TENSOR(2, true, o_outputs);

    /* CHOOSE EXECUTER AND CONTEXT HERE */
    /* YOU MAY USE THE GENERATED RESOLVER */

    // ctx->executer = resolve_operator__ai_onnx_preview_training__adagrad__1(ctx);
    // ctx->executer_context = op_ctx;

    TRACE_EXIT(1);

    /* CHANGE RETURN CODE IF THIS PREPARER IS VALID */
    return OP_ENOSYS;
    // return OP_OK;
}